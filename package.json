{
  "name": "professor-portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "index.html",
  "scripts": {
    // "build": "rm -rf dist && mkdir dist && cp -r public/* dist/ && cp index.html dist/",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    // "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://pranaycs24.github.io/portfolio/"
}
